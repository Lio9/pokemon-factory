# nginx 文件说明
# 所属模块：前端应用。
# 文件类型：部署与运行配置文件。
# 核心职责：负责容器、代理或运行环境相关的基础配置。
# 阅读建议：建议关注端口、路径与镜像/进程行为设置。
# 项目注释补全说明：本注释用于帮助后续维护时快速定位文件在整体架构中的职责。

server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}