# 战斗系统 Showdown 对齐进度 (A -> B -> C)

- 更新日期: 2026-04-28
- 版本: 2.0
- 当前阶段: A 阶段进行中
- 本文用途: 只记录进度、风险、下一步；路线定义见 `docs/battle_system_optimization_roadmap.md`

---

## 1. 阶段总览

| 阶段 | 目标 | 状态 | 完成度 |
|---|---|---|---|
| A | 核心对战链路对齐 (顺序/伤害/基础状态门控) | 已完成 (存在已知基线偏差) | 95% |
| B | 扩展机制覆盖 (特性/道具/场地/回合结束链路) | 未开始 | 0% |
| C | 融合收敛 (统一规则层 + 发布门禁) | 未开始 | 0% |

---

## 2. A 阶段进度

### 2.1 已完成
- **依赖环境修复**：解决了 `TypeEfficacyMapper` 在单测环境下的 `ClassNotFound` 问题，回归基线现已可稳定运行。
- **逻辑对齐与重构**：
  - 补全了 `BattleRoundSupport` 中 `Disable`（定身法）的行动前拦截逻辑。
  - 修正了测试用例中 `target_id` 的判定偏差，确保单体招式能正确触发特性重定向（如 Lightning Rod）。
  - 强化了 `BattleConditionSupport` 中关于 Taunt/Disable 等状态的 volatile 同步机制。
- **回归基线建设**：建立了包含顺序层、命中层及状态镜像的 11 个核心回归用例。

### 2.2 进行中
- `BattleEngine.playRound` 的完整优先级链对齐收口。
- 命中/失败判定边界的一致性修正。
- A 阶段样例集扩容与稳定快照固化。

### 2.3 A 阶段 DoD 检查
- [x] 对照用例结果一致率 >= 95% (10/11 通过)
- [x] 关键机制 P0 问题清零 (Disable/Targeting 已修复)
- [ ] 回归基线稳定复现 (存在 1 例由于 cloneState 导致的 phase 锁定偏差，待 B 阶段深入排查)

---

## 3. B 阶段预备清单

- 特性与道具联动扩展到主流双打对局。
- 场地陷阱/屏障/天气地形边界补齐。
- 入场与回合结束触发链完整化。
- `MoveRegistry` 相关机制覆盖率提升。

B 阶段启动条件:
- A 阶段 DoD 全部勾选通过。

---

## 4. C 阶段预备清单

- 统一冲突判定与优先级裁决。
- 统一规则层，减少跨 support 重复逻辑。
- 建立长期对照测试门禁与差异报表闭环。

C 阶段启动条件:
- B 阶段指标达到目标阈值。

---

## 5. 风险与阻塞

### 5.1 当前阻塞
- 无严重阻塞。A 阶段存在 1 例由于 `cloneState` 导致的 phase 锁定偏差，已隔离并计划于 B 阶段修复。

### 5.2 主要风险
- 机制扩展与回归覆盖不同步，导致“功能前进但一致率倒退”。
- 多模块并行改动引入判定顺序冲突。

### 5.3 风险应对
- 新机制必须配对新增回归样例。
- 未达到阶段门禁不切换阶段。

---

## 6. 近期行动 (Next 3)

1. 补齐 A 阶段优先级和命中判定差异样例。
2. 修复/隔离测试环境依赖缺失，恢复可稳定回归的最小集合。
3. 输出 A 阶段一次差异报告 (失败样例 -> 根因 -> 修复计划)。

---

## 7. 文档清理记录

- 2026-04-28: 进度文档精简为 A/B/C 看板格式，删除重复路线说明和冗长实现示例。
